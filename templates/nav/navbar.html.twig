<nav class="navbar navbar-inverse">
    <div class="container-fluid">
        <div class="navbar-header">
            <a class="navbar-brand" href="#">ENI ECOLE</a>
        </div>
        <ul class="nav navbar-nav">
            <li class="active"><a href="{{ path('list')}}">Accueil</a></li>
        </ul>
        <ul class="nav navbar-nav navbar-right">
            {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                <li class="active"><a class="bienvenue" href="#">Bienvenue {{ app.user.username }}</a></li>
                <li><a href="{{ url('logout') }}"><span class="glyphicon glyphicon-log-out"></span> Déconnexion</a></li>
            {% else %}
                <li><a href="{{ url('connect') }}"><span class="glyphicon glyphicon-log-in"></span> Connexion</a></li>
            {% endif %}
        </ul>
    </div>
</nav>