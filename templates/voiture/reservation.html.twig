{% extends 'base.html.twig' %}


{% block title %}Ajout voiture{% endblock %}

{% block body %}
    <h1>Reservation voiture : {{ voiture.Reference }}
    </h1>
    <div class="col-lg-4 col-md-2 col-sm-2"></div>


    <div class="col-lg-4 col-md-8 col-sm-8 col-12">
        {{ form_start(form) }}
        {{ form_widget(form) }}

        <button type="submit" class="btn btn-primary center-block">Reserver la voiture</button>

        {{ form_end(form) }}
    </div>
{% endblock %}
